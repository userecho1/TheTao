# Copilot 默认指令（极简）

## 加载顺序（MUST）
1. 先读 `AGENTS.md`（治理锚点唯一源）。
2. 再读 `.github\instructions\world-rules.instructions.md`（动态执行规则）。

## 短约束（MUST）
- 仅按 `AGENTS.md` + Shu 动态规则执行；冲突按 `AGENTS.md` 裁决。
- fail-close、工件演进 gate、授权分级均引用 `AGENTS.md`，不在此重复展开。
- 输出保持简短、可执行、可验证。
